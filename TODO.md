- [x] if you select multiple fields and open the sidebar you don't need to click
  aggregate.
- [x] change the cursor icon if you are move columns or holding for moving
- [x] click, hold and drag to select cases doesn't work
- [x] combine left click and right click on column dialog to the left click
- [x] you should be able to close all tabs
- [x] add graphic view for explain (live the entity relation view)
- [x] add functions to `new` on the left click
- [x] create scripts to init databases and docker-compose to start multiple postgres and mysql
- [x] makefile to run test, projects...
- [x] make it possible to load connections and workspaces via xml files. also
  saving them should be easilly transforable to new installations.
- [x] opening a workspace should not open the app in background. it should be on top.
- [x] create a demo db for every engine, not just SQLite (in-app, from the
  connection dialog)
- [x] save schemas to use for later (capture a database's structure under a
  name, reopen it in a console)
- [x] handle PostgreSQL's many schemas per database (profile field, console
  dropdown; MySQL needs none — schema == database there)
