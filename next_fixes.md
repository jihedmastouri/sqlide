Now:
- [x] the data grid should be like objects:
    - [x] drag and drop to reorder columns (was already in — header drag + cell-menu Move Left/Right)
    - [x] bug: re-ordering columns is undone if you sort (order by) by a column
    - [x] click-hold-drag to select cases (was already in — primary-button drag selects a block)
- [x] Add advanced connection settings: ssh tunnel, ssl certificates....
- [x] in the table-relation diagram can we move tables around?
- [x] Query Builder

Future fixes:
- [ ] hover table name to show DDL still not working
- [ ] Tabs in split view should be able to close them
- [ ] max 3 split views. and check scroll and size, it's broken
